## Design Context

This project uses the `impeccable` skill for frontend design work.

- **Register:** brand (marketing site) · **Platform:** web (Next.js Pages Router)
- Read `PRODUCT.md` (strategy: users, positioning, brand personality, anti-references) and `DESIGN.md` (visual system: colors, typography, components) before making design decisions.
- Live variant iteration is pre-configured — `/impeccable live` boots straight in.
